{{ form_start(form) }}

    {{ form_widget(form) }}

        <button class="btn btn-primary mt-4">{{ button_label|default('Enregistrer') }}</button>

        <a class="btn btn-primary mt-4 mx-4" href="{{ path('fournisseur_index') }}">Retour</a>
{{ form_end(form) }}
