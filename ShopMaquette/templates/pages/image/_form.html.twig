{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-3">{{ button_label|default('Sauvegarder') }}</button>

        <a class="btn btn-primary mt-3" href="{{ path('app_image_index') }}">Retour</a>
{{ form_end(form) }}
